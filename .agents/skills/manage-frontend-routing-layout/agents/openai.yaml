interface:
  display_name: "Manage Frontend Routing Layout"
  short_description: "Frontend guidance for manage frontend routing layout"
  default_prompt: "Use $manage-frontend-routing-layout to handle this ArchivalProcessing frontend task with the correct context and verification."
