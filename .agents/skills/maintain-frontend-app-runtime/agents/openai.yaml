interface:
  display_name: "Maintain Frontend App Runtime"
  short_description: "Frontend guidance for maintain frontend app runtime"
  default_prompt: "Use $maintain-frontend-app-runtime to handle this ArchivalProcessing frontend task with the correct context and verification."
