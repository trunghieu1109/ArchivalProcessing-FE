interface:
  display_name: "Manage Frontend Backup"
  short_description: "Frontend guidance for manage frontend backup"
  default_prompt: "Use $manage-frontend-backup to handle this ArchivalProcessing frontend task with the correct context and verification."
