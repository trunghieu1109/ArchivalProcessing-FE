interface:
  display_name: "Manage Frontend Folder Upload"
  short_description: "Frontend guidance for manage frontend folder upload"
  default_prompt: "Use $manage-frontend-folder-upload to handle this ArchivalProcessing frontend task with the correct context and verification."
