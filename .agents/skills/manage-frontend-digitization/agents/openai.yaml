interface:
  display_name: "Manage Frontend Digitization"
  short_description: "Frontend guidance for manage frontend digitization"
  default_prompt: "Use $manage-frontend-digitization to handle this ArchivalProcessing frontend task with the correct context and verification."
