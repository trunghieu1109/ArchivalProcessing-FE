interface:
  display_name: "Manage Frontend Document Locks"
  short_description: "Frontend guidance for manage frontend document locks"
  default_prompt: "Use $manage-frontend-document-locks to handle this ArchivalProcessing frontend task with the correct context and verification."
