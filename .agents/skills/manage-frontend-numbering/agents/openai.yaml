interface:
  display_name: "Manage Frontend Numbering"
  short_description: "Frontend guidance for manage frontend numbering"
  default_prompt: "Use $manage-frontend-numbering to handle this ArchivalProcessing frontend task with the correct context and verification."
