interface:
  display_name: "Build Deploy Frontend"
  short_description: "Frontend guidance for build deploy frontend"
  default_prompt: "Use $build-deploy-frontend to handle this ArchivalProcessing frontend task with the correct context and verification."
