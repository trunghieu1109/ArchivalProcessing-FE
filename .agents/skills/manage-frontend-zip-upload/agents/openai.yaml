interface:
  display_name: "Manage Frontend Zip Upload"
  short_description: "Frontend guidance for manage frontend zip upload"
  default_prompt: "Use $manage-frontend-zip-upload to handle this ArchivalProcessing frontend task with the correct context and verification."
